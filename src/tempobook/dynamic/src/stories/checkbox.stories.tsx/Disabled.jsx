
            import { Disabled } from "./../../../../../stories/checkbox.stories.tsx";

            const TempoComponent = () => {
              return Disabled.render(Disabled.args);
            }

            TempoComponent.getLayout = (page) => page;

            export default TempoComponent;