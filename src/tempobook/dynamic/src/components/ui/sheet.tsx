
            import { SheetTitle } from "./../../../../../components/ui/sheet.tsx";

            const TempoComponent = () => {
              return <SheetTitle />;
            }

            TempoComponent.getLayout = (page) => page;

            export default TempoComponent;